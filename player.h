// NAME : RAJ PRATEEK KOSARAJU

// for Player triangle

typedef struct player{  //The player triangle struct
	int c;
	int r;
	int height;
	u16 color;
} Player;

Player drawPlay(Player play);
void hidePlay(Player play);

